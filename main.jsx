import React from 'react'
import ReactDOM from 'react-dom/client'
import PianoApp from './piano-app'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <PianoApp />
    </React.StrictMode>,
)
